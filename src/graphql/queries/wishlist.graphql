# import '/fragments/product.graphql'
# import '/fragments/user.graphql'

query fetchUsersWhishlist {
    findOneWishlist {
      payload {
        owner {
          ...user
        }
        products {
          ...product
        }
      }
    error
  }
}
