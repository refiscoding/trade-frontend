# import '/fragments/product.graphql'
query fetchUsersCart {
  findCart {
    payload {
        owner {
            email
            username
        }
        productsQuantities {
            quantity
            product {
                ...product
            }
        }
        total
    }
    error
  }
}
