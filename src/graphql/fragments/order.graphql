# import '/fragments/transaction.graphql'
# import '/fragments/product.graphql'

fragment order on Order {
  id
  created_at
  updated_at
  orderNumber
  orderDate
  paidDate
  deliveryDate
  count
  orderTotal
  wasReturned
  orderStatus
  signatory {
    id
    name
    relation
    isCustomer
  }
  items {
    id
    quantity
    total
    products {
      ...product
    }
  }
  deliveryAddress {
    ...address
  }
}
